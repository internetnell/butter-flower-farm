<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Product Page</title>
    <style>
        .product-container {
            max-width: 400px;
            margin: 40px auto;
            padding: 24px;
            border: 1px solid #eee;
            border-radius: 8px;
            background: #fff;
            box-shadow: 0 2px 8px rgba(0,0,0,0.04);
            text-align: center;
            font-family: Arial, sans-serif;
        }
        .product-image {
            width: 100%;
            max-width: 320px;
            height: auto;
            border-radius: 6px;
            margin-bottom: 20px;
        }
        .product-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 10px;
            color: #222;
        }
        .product-price {
            font-size: 1.2rem;
            color: #27ae60;
            margin-bottom: 20px;
        }
        .add-to-cart-btn {
            background: #0070f3;
            color: #fff;
            border: none;
            padding: 12px 28px;
            border-radius: 4px;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.2s;
        }
        .add-to-cart-btn:hover {
            background: #005bb5;
        }
    </style>
</head>
<body>
    <div class="product-container">
        <img class="product-image" src="https://via.placeholder.com/320x240?text=Product+Image" alt="Product Image">
        <div class="product-title">{{ product.title | default: "Sample Product Title" }}</div>
        <div class="product-price">${{ product.price | default: "29.99" }}</div>
        <form method="post" action="/cart/add">
            <input type="hidden" name="id" value="{{ product.id | default: '123456' }}">
            <button type="submit" class="add-to-cart-btn">Add to Cart</button>
        </form>
    </div>
</body>
</html>