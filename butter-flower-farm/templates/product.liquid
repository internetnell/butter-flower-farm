<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Product Page</title>
    <style>
        .product-container {
            max-width: 400px;
            padding: 24px;
            text-align: left;
            font-family: Arial, sans-serif;
        }
        .product-image {
            width: 100%;
            max-width: 320px;
            height: 320px;
            border-radius: 6px;
            margin-bottom: 20px;
        }
        .product-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 10px;
            color: #222;
        }
        .product-price {
            font-size: 1.2rem;
            color: #27ae60;
            margin-bottom: 20px;
        }
        .add-to-cart-btn {
            background: #0070f3;
            color: #fff;
            border: none;
            padding: 12px 28px;
            border-radius: 4px;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.2s;
        }
        .add-to-cart-btn:hover {
            background: #005bb5;
        }
    </style>
</head>
<body>
    <div class="product-container">
        <img class="product-image" src="{{ product.featured_image | image_url: width: 800 }}" alt="Product Image">
        <div class="product-title">{{ product.title | default: "Sample Product Title" }}</div>
        <div class="product-price">{{ product.price | money_without_trailing_zeros }}</div>
        <form method="post" action="/cart/add">
            <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id | default: '123456' }}">
            <button type="submit" class="add-to-cart-btn">Add to Cart</button>
        </form>
    </div>
</body>
</html>